.DS_Store
Thumbs.db
*.log
.netlify/
.vscode/
.idea/

# Keep raw, unexported source material out of the repo — only the finished
# web-ready files in /audio and /media belong here.
*.wav.original
/audio/_raw/
/media/_raw/

# Full-resolution reference images — only the web-sized copies in
# media/scatter/ belong in the repo.
/Site design inspiration/
